import React from "react";

import "./Sidebar.scss";

const Sidebar = () => {
  return (
    <div className="Sidebar">
      <button>Next Text Block</button>

      <button>Zoom In</button>

      <button>Zoom Out</button>

      <button>Rotate</button>
    </div>
  );
};

export default Sidebar;
